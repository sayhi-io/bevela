# Luna Study03: fresh attempt v2

> Historical source recovery. Historical v2 pre-launch addendum, superseded as the active protocol by later native-seam studies. This recovery does not assert that its planned arms launched, completed or passed.
>
> Status and authorization statements below describe their original writing;
> they are not current launch permission or new validation claims. See
> [Results](../RESULTS.md) for the maintained result index. Archive references
> identify private evidence, not public downloads; no raw artifacts are included.
> This sanitized documentation copy is not a replacement for a historical sealed
> input. Original frozen source and evidence remain unchanged in their archives.

This pre-launch addendum supersedes the original protocol's execution paths and
browser-oracle version only. The original protocol, contract, scored artifacts,
invalidated result and original runner/evaluator remain unchanged. The user's
2026-09-08 instruction authorizes this fresh Luna/medium attempt.

Historical private archive ID: `20260908-luna-release-03-v2`.
Runner: `python -m experiments.luna_release_study_v2`; evaluation:
`experiments/luna_release_evaluation_v2.py`. These versioned copies preserve v1.
Fresh order: A2-control, A2-aware, B2-aware, B2-control. No old candidate becomes
a new score. A2/B2 identifiers also give native PI records and session homes
distinct identities. The same four owners, revision checkpoint, storage-owner
successor, integrator, correction limits and Luna-medium reviewer are retained.

All acceptance content is identical except that the release browser entrypoint
is the already independently reviewed `luna_release_browser_v2.py`. It inherits
seven original tests and replaces the navigation oracle with pre-captured IDs.
Both the inherited source and v2 override are frozen and copied to every arm.
No product source, acceptance criterion, owner budget or reasoning effort changes.

The binary used for v1 Codex is copied from its preserved tool directory. Browser
launch uses a common wrapper that adds `--disable-gpu` and
`--disable-software-rasterizer`; both wrapper and native binary hashes are frozen.
This equal SVG/CSS CPU rendering configuration avoids a reproduced DGX compositor
stall without forced clicks, synthetic frame timers or weakened assertions.
The wrapper is shared by worker preflight, candidate tests and independent gates.
All other tool/context settings stay as declared in the original protocol.
Model weights remain an unpinned alias: no immutable-weight comparison is claimed.

Fresh control/aware actual-worker preflights and same-session resume checks are
required. Fresh untouched-baseline gates must retain passing legacy/regression
tests and expected failures for absent release features. The prior paired
rehearsal reviews remain unscored preparation, referenced with exact hashes rather
than copied or relabeled as new runs. The v2 navigation repair's prior 80/80
unchanged-candidate calibration remains diagnostic only. Freeze checks preserved
v1 input hashes and the repair receipt. The new exact input set needs independent
Luna-medium harness review with no unresolved blockers before scored launch.

Run preparation and all four scored arms with the explicit CLI actions. Existing
directories/artifacts are never overwritten; native uncertainty and operational
failure stop the affected action for reconciliation. Do not restart failed model
sessions automatically or turn a harness failure into a PI/model score.
Publish arm-level failures and measured usage before pairwise interpretation.
No statistical significance or general PI advantage is inferable from two pairs.
No production, canonical source integration, remote Git or UI activation is included.

After freeze, `python -m experiments.luna_release_sequence_v2` launches the
predeclared sequence once, including independent gates and Luna-medium review
between arms. An exclusive sequence marker prevents an accidental second launch.
Missing transport/model verification or valid review stops with retained evidence;
ordinary delivered candidate failure remains an outcome, not an automatic retry.
The driver source is part of the frozen input map. `results.json` is partial until
every arm's independent review completes.
